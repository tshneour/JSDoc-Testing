name: GitHub pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        uses: andstor/jsdoc-action@v1.2.1
        with:
          source_dir: ./folder
          recurse: true
          output_dir: ./out